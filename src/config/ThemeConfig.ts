
/**
 * Legacy ThemeConfig compatibility
 * Re-export the current theme configuration for any older references.
 */

import { themes } from './themes';

export const ThemeConfig = themes;

export default ThemeConfig;

